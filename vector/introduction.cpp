#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> vec;  
    // EN: Create an empty vector of integers  
    // BN: খালি ইন্টিজার ভেক্টর তৈরি

    vector<int> value = {1, 2, 3, 4, 5};  
    // EN: Create a vector and initialize with given values  
    // BN: নির্দিষ্ট মান দিয়ে ভেক্টর তৈরি

    vector<int> value2(3, 0);  
    // EN: Create a vector with size 3, all elements initialized to 0  
    // BN: ৩টি এলিমেন্টসহ ভেক্টর তৈরি, প্রতিটি মান ০

    return 0;
}
